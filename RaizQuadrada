#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main (){
	
	int x;
	int y; 
	float i;
	float h;
	
	
	printf("Digite X: ");
	scanf("%d", &x);
	printf("Digite Y: ");
	scanf("%d", &y);
	
	i =(x*x) + (y*y);
	h = sqrt(i);
	
	printf("Valor: %.1f", h);
	
	system("pause");
}
